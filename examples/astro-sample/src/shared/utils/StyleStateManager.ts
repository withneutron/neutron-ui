/** Stores style managers, watches for condition changes, and updates element styles as needed */
class StyleStateManager {
  private elements = {}
}

const styleStateManager = new StyleStateManager()

export default styleStateManager
