export const tooltipStyles = {
  bg: "$neutral11",
  radius: "$tooltip",
  boxShadow: "$medium",
  color: "$textNeutral11",
  fontSize: "$small",
  lineHeight: "$listItem",
  maxWidth: "$18",
  position: "$absolute",
  px: "calc($tooltipBaseP + $2)",
  py: "$tooltipBaseP",
  zIndex: "$max",
}

export const tooltipArrowStyles = {
  pointerEvents: "none",
}
